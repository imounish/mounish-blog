---
// T6: port of src/components/partials/SectionTop.jsx.
interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---

<div class={`mb-8 mt-3 lg:mt-4 ${className || ''}`}>
  <slot />
</div>
