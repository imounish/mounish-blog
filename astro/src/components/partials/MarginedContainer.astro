---
// T6: port of src/components/partials/MarginedContainer.jsx.
interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---

<div class={`container mx-auto ${className || ''}`}>
  <slot />
</div>
