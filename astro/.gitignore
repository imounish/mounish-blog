# build output
dist/
# generated types
.astro/

# dev-mode copy of the Pagefind index, synced from dist/pagefind by
# scripts/sync-pagefind-dev.mjs (see package.json's predev/pagefind:sync
# scripts) so `astro dev` has something to serve at /pagefind/pagefind.js.
# This is a build artifact snapshot, not source.
public/pagefind/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/
